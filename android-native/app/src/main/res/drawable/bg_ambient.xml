<?xml version="1.0" encoding="utf-8"?>
<!-- 环境光背景渐变 — 对应 Web body::after 的 radial-gradient ambient -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <gradient
        android:type="radial"
        android:gradientRadius="120%p"
        android:centerX="50%"
        android:centerY="0%"
        android:startColor="@color/bg_ambient_top_dark"
        android:endColor="@color/bg_primary_dark" />
</shape>