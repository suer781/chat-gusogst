<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingVertical="3dp">

    <!-- 思考过程 -->
    <LinearLayout
        android:id="@+id/thinkingBlock"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        android:background="@drawable/bg_bubble_assistant"
        android:padding="@dimen/space_md"
        android:layout_marginBottom="@dimen/space_xs">
        <TextView android:id="@+id/tvThinkingLabel" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="@string/chat_thinking" android:textSize="@dimen/text_xs" android:textColor="@color/text_tertiary" />
        <TextView android:id="@+id/tvThinkingContent" android:layout_width="wrap_content" android:layout_height="wrap_content" android:textSize="@dimen/text_sm" android:textColor="@color/text_secondary" android:lineSpacingMultiplier="1.3" />
    </LinearLayout>

    <!-- 工具调用容器 -->
    <LinearLayout
        android:id="@+id/toolCallsContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone" />

    <!-- 消息正文 -->
    <TextView
        android:id="@+id/tvMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_bubble_assistant"
        android:paddingHorizontal="@dimen/bubble_padding_h"
        android:paddingVertical="@dimen/bubble_padding_v"
        android:textColor="@color/bubble_assistant_text"
        android:textSize="@dimen/text_base"
        android:lineSpacingMultiplier="1.4" />

    <!-- 操作按钮 -->
    <LinearLayout
        android:id="@+id/actionBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="@dimen/space_xs"
        android:paddingStart="@dimen/bubble_padding_h"
        android:visibility="gone">

        <TextView android:id="@+id/btnCopy" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="@string/chat_copy" android:textSize="@dimen/text_xs" android:textColor="@color/text_tertiary" android:paddingHorizontal="@dimen/space_sm" android:paddingVertical="@dimen/space_xs" android:background="?attr/selectableItemBackground" />
        <TextView android:id="@+id/btnRegen" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="@string/chat_regen" android:textSize="@dimen/text_xs" android:textColor="@color/text_tertiary" android:paddingHorizontal="@dimen/space_sm" android:paddingVertical="@dimen/space_xs" android:background="?attr/selectableItemBackground" />
        <TextView android:id="@+id/btnDelete" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="@string/chat_delete" android:textSize="@dimen/text_xs" android:textColor="@color/text_tertiary" android:paddingHorizontal="@dimen/space_sm" android:paddingVertical="@dimen/space_xs" android:background="?attr/selectableItemBackground" />
    </LinearLayout>
</LinearLayout>
